# Tasks done in LAB 2

- Played around with sampling and quantization on the lena image — tried a mild setting (barely noticeable change) and an aggressive one (looked blocky and almost black & white).
- Took the lena and cameraman images, resized them to match, then subtracted one from the other and brightened one by adding a constant of 175.
- Loaded two mask images (A and B) and combined them using basic set logic — union, difference, symmetric difference, and intersection.
- Used OpenCV, NumPy, Matplotlib, and Pillow throughout.
